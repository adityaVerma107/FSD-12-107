# EventLoop 

JS is a synchronous and single threaded byfault 
 
 ## there can be async behaviour 

-with  browserAPI-
setTimeout ,setInterval , setImmediate , nextTick<br>
-with Promises<br>
-with Event handlers

## promise 
a function not executed immediately that it must be executed after a while it has some status during the execution 

### Important Topic 
-promises<br>
-synchr and asynchr=

modern  JS is divided into 2 categories 
1. common Js (cjs) -> support OOPS -> require
2. Module JS (.mjs) -> follow modular approach -> import